<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permesso Health Connect per scrivere la glicemia -->
    <uses-permission android:name="android.permission.health.WRITE_BLOOD_GLUCOSE" />

    <application
        android:label="Glucose Bridge"
        android:icon="@mipmap/ic_launcher"
        android:exported="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Activity “rationale” (richiesta dal flusso permessi Health Connect) -->
        <activity
            android:name=".PermissionsRationaleActivity"
            android:exported="true" />

        <!-- Alias richiesto per mostrare l’uso permessi nella UI di Health Connect -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".PermissionsRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

    </application>

</manifest>
